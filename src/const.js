const PHOTO_MAX_QTY = 12
const PHOTO_MIN_SIZE = 10
const PHOTO_MAX_SIZE = 1 * 1000 * 1000 * 10 // 10MB

const VIDEO_MAX_QTY = 1
const VIDEO_MIN_SIZE = 10
const VIDEO_MAX_SIZE = 1 * 1000 * 1000 * 200 // 200MB

const BOOK_MAX_QTY = 1
const BOOK_MIN_SIZE = 10
const BOOK_MAX_SIZE = 1 * 1000 * 1000 * 50 // 50MB

export {
  PHOTO_MAX_QTY,
  PHOTO_MAX_SIZE,
  PHOTO_MIN_SIZE,
  VIDEO_MAX_QTY,
  VIDEO_MAX_SIZE,
  VIDEO_MIN_SIZE,
  BOOK_MAX_QTY,
  BOOK_MAX_SIZE,
  BOOK_MIN_SIZE,
}
